---
interface Props {
  class?: string;
  dark?: boolean;
}

const { class: className, dark = false } = Astro.props;
---

<div
  class:list={[
    "pt-5 px-[10%] pb-5 lg:pb-9 relative",
    { "bg-mainPurple": dark },
    className,
  ]}
>
  <slot />
</div>
