---
interface Props {
  class?: string;
  dark?: boolean;
  noPadding?: boolean;
  id?: string;
}

const { class: className, dark = false, noPadding, id: idValue } = Astro.props;
---

<div
  id={idValue}
  class:list={[
    "px-[10%] relative",
    { "bg-mainPurple": dark },
    { "pt-5 pb-5 lg:pb-9": !noPadding },
    className,
  ]}
>
  <slot />
</div>
